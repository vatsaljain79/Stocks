services:
  - type: web
    name: golden-harbor-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
      - key: SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: golden-harbor-db
          property: connectionString

databases:
  - name: golden-harbor-db
    plan: free
